name: detectron2
channels:
  - conda-forge
dependencies:
  - numpy
  - pywin32
  - cudatoolkit
  - pytorch
  - torch
  - torchvision
  - git
  - pip
  - pip:
    - git+https://github.com/facebookresearch/detectron2.git@v0.3
    - install -e detectron2
